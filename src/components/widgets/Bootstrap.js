'use strict';

import React from 'react';
import {Badge as BootBadge, Label as BootLabel} from 'react-bootstrap';

export const Badge = (props) => <BootBadge {...props}>徽章</BootBadge>

export const Label = (props) => <BootLabel {...props}>标签</BootLabel>
